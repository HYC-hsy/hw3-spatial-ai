# HW3 作业说明

项目根目录：`/mnt/data/kw/hy/projects/course/DL/hw3`

更新时间：2026-06-20

## 总体完成情况

本项目的主要技术任务已经完成：

1. 第一题 3DGS/AIGC 场景融合已完成。
   - Flowers 背景 3DGS 已训练到 30,000 iteration。
   - Object A 已有点云 PLY。
   - Object B 已用 threestudio/SDS 从 prompt 训练并导出 OBJ。
   - Object C 已用 Zero123 从单张图训练并导出 OBJ。
   - 已将背景 3DGS、Object A、Object B、Object C 导入 Blender 并渲染最终融合视频。

2. 第二题 ACT/LeRobot 训练与离线泛化评估已完成。
   - ACT-A：用环境 A 训练 50,000 step。
   - ACT-ABC：用环境 A/B/C 合并数据训练 50,000 step。
   - 两个模型均在环境 D demonstration 上做了 zero-shot offline action-loss evaluation。
   - ACT-ABC 在 D 上的平均 action loss 低于 ACT-A，说明多环境训练改善了跨环境泛化。

3. 报告已生成。
   - LaTeX 源文件：`report/main.tex`
   - PDF 报告：`report/main.pdf`
   - 报告图表：`report/figures/`、`report/tables/`

## 仍需注意或未完成的事项

严格说，实验和代码产物已经完成；但提交前还有几项人工信息或限制需要注意：

1. `report/main.tex` 里 GitHub repository、姓名、学号、云端模型权重链接仍需要按课程提交要求手动填写。
2. CALVIN 没有做真实 simulator rollout success rate，因为当前运行中没有完整可用的 CALVIN simulator rollout 环境；报告中使用环境 D demonstrations 的 offline action loss 作为 zero-shot proxy 指标，并已明确写出 `Success rate: Not rolled out`。
3. Flowers 背景 3DGS 的 `metrics.py` 生成了 `results.json`，但 SSIM/PSNR/LPIPS 是 NaN。原因是该 Mip-NeRF 360 split 没有被脚本识别到 held-out test cameras；这不影响 30K 背景 PLY、训练渲染图和最终融合视频的生成。
4. `outputs/fusion/fusion_walkthrough.mp4` 文件较小，但 `outputs/fusion/fusion_scene.blend` 为 691MB，日志确认四个资产都已导入：背景 PLY、Object A PLY、Object B OBJ、Object C OBJ。
5. `external/`、`.conda_envs/`、`.cache/` 体积较大，属于本地可复现实验环境和下载缓存；提交代码仓库时通常不应全部提交，提交服务器产物或压缩包时再按课程要求筛选。

## 关键结果文件

### 第一题 3DGS/AIGC

- 背景 3DGS PLY：`outputs/3dgs_background/flowers/point_cloud/iteration_30000/point_cloud.ply`
- 背景 3DGS 渲染图：`outputs/3dgs_background/flowers/train/ours_30000/renders/`
- Object A PLY：`outputs/object_a/point_cloud/iteration_30000/point_cloud.ply`
- Object B SDS OBJ：`outputs/text_asset_b_real/export/model.obj`
- Object B SDS MTL：`outputs/text_asset_b_real/export/model.mtl`
- Object B 训练视频：`outputs/text_asset_b_real/object_b_sds/robot_prompt@20260619-144634/save/it10000-test.mp4`
- Object C Zero123 OBJ：`outputs/image_asset_c_real/export/model.obj`
- Object C Zero123 MTL：`outputs/image_asset_c_real/export/model.mtl`
- Object C 测试视频：`outputs/image_asset_c_real/object_c_zero123/chair_image@20260619-151900/save/it600-test.mp4`
- 融合配置：`configs/fusion_scene.yaml`
- 融合 Blender 文件：`outputs/fusion/fusion_scene.blend`
- 融合视频：`outputs/fusion/fusion_walkthrough.mp4`
- 第一题状态清单：`TASK1_STATUS.md`

### 第二题 ACT/LeRobot

- ACT-A 50K checkpoint：`outputs/act_env_a/checkpoints/050000/pretrained_model/model.safetensors`
- ACT-ABC 50K checkpoint：`outputs/act_env_abc/checkpoints/050000/pretrained_model/model.safetensors`
- ACT-A 环境 D 离线评估：`outputs/eval_env_d_act_a/offline_metrics.json`
- ACT-ABC 环境 D 离线评估：`outputs/eval_env_d_act_abc/offline_metrics.json`
- ACT-A 训练日志：`outputs/act_env_a_train.log`
- ACT-ABC 训练日志：`outputs/act_env_abc_train.log`
- 报告训练曲线 CSV：`report/tables/act_env_a_train_metrics.csv`、`report/tables/act_env_abc_train_metrics.csv`
- 报告结果表：`report/tables/act_results.csv`

### 主要数值结果

- ACT-A on D：mean loss = `0.16487825646285806`，throughput = `172.45 frames/s`
- ACT-ABC on D：mean loss = `0.14512578789136882`，throughput = `172.66 frames/s`
- 结论：ACT-ABC 的 D 环境 action loss 更低，说明 A/B/C 多环境训练比只用 A 训练有更好的 zero-shot 泛化。

## 顶层文件说明

- `HW3_深度学习与空间智能.pdf`：课程作业原始说明 PDF。
- `README.md`：项目 README。
- `TASK1_STATUS.md`：第一题 3DGS/AIGC 工具链、训练结果和复现命令的状态说明。
- `作业说明.md`：本文档，说明目录结构、文件职责、完成情况和未完成/注意事项。
- `environment.yml`：基础 Conda 环境描述。
- `pyproject.toml`：本项目 Python 包配置。
- `.env.hw3_setup`：统一环境变量，指定 `HW3_ROOT`、HF/PIP/TORCH/CONDA 缓存路径、PATH 等，保证下载和缓存尽量放在项目目录下。
- `.gitignore`：Git 忽略规则。
- `.DS_Store`：macOS 生成的隐藏文件，无作业功能。

## 顶层目录说明

- `.cache/`：项目本地缓存目录。包含 pip、huggingface、torch、conda packages、临时目录等，目的是避免写到用户全局缓存或系统目录。
- `.conda_envs/`：项目本地 Conda 环境。
- `assets/`：第一题 Object A/Object C 等输入素材。
- `configs/`：训练、评估、融合场景配置文件。
- `data/`：数据集目录，包含 Mip-NeRF 360 Flowers 和 CALVIN/LeRobot 数据。
- `external/`：外部依赖源码、二进制和模型工具，如 Blender、gaussian-splatting、threestudio、LeRobot、Zero123 相关内容。
- `logs/`：安装、训练、渲染、检查过程的日志。
- `outputs/`：所有实验输出，包括 checkpoints、评估结果、3D 资产、融合视频等。
- `processed/`：预处理后的中间数据，例如 CALVIN 转 LeRobot 的准备产物。
- `report/`：LaTeX 报告、PDF、图像和表格。
- `scripts/`：复现、训练、评估、检查、融合渲染脚本。
- `src/`：本项目 Python 源码。

## `.cache/` 目录说明

- `.cache/requirements_threestudio_pypi.txt`：安装 threestudio 时记录/使用的 PyPI requirements。
- `.cache/requirements_threestudio_pypi_core.txt`：threestudio 核心 PyPI requirements。
- `.cache/conda_pkgs/`：Conda 包缓存。
- `.cache/huggingface/`：Hugging Face 模型缓存，例如 Stable Diffusion 相关文件。
- `.cache/pip/`：pip 下载缓存。
- `.cache/tmp/`：临时文件目录。
- `.cache/torch/`：torch/hub 缓存。
- `.cache/xdg/`：XDG 缓存。

## `.conda_envs/` 目录说明

- `.conda_envs/colmap_blender/`：COLMAP、Blender 运行依赖、ffmpeg、pyyaml 等工具环境。
- `.conda_envs/threestudio/`：threestudio、SDS、Zero123、3DGS CUDA 扩展、PyTorch CUDA 等运行环境。

## `assets/` 目录说明

- `assets/SOURCE.txt`：素材来源说明。
- `assets/object_a/`：Object A 相关输入或中间素材目录。
- `assets/object_c/`：Object C 单图输入目录，关键输入为 `assets/object_c/input.png`。

## `configs/` 文件说明

- `configs/act_env_a.yaml`：ACT 环境 A 训练的早期/基础配置。
- `configs/act_env_a_real.yaml`：ACT-A 正式 50K 训练配置。
- `configs/act_env_a_smoke.yaml`：ACT-A smoke test 配置。
- `configs/act_env_abc.yaml`：ACT A/B/C 合并训练的早期/基础配置。
- `configs/act_env_abc_real.yaml`：ACT-ABC 正式 50K 训练配置。
- `configs/fusion_scene.yaml`：Blender 融合场景配置，指定背景 3DGS PLY、Object A PLY、Object B OBJ、Object C OBJ、位置、旋转、缩放、输出分辨率等。
- `configs/paths.yaml`：项目路径配置。
- `configs/text_to_3d.yaml`：Object B text-to-3D prompt、Stable Diffusion 模型和推荐训练步数配置。

## `data/` 目录说明

- `data/360_extra_scenes/flowers/`：Mip-NeRF 360 Flowers 数据，包含 images、downsampled images、COLMAP sparse 相机和点云文件。
- `data/360_extra_scenes/treehill/`：另一个 Mip-NeRF 360 场景，当前报告主要使用 Flowers。
- `data/calvin_task_ABC_D/`：CALVIN LeRobot 格式数据。
- `data/calvin_task_ABC_D/calvin_task_ABC_D_lerobot_0_4/`：环境 A 数据。
- `data/calvin_task_ABC_D/calvin_task_ABC_D_lerobot_1_4/`：环境 B 数据。
- `data/calvin_task_ABC_D/calvin_task_ABC_D_lerobot_2_4/`：环境 C 数据。
- `data/calvin_task_ABC_D/calvin_task_ABC_D_lerobot_3_4/`：环境 D 数据，用于 zero-shot offline evaluation。

## `external/` 目录说明

- `external/3dgs_ext_src/`：单独下载并编译的 3DGS CUDA 扩展源码，包括 `diff-gaussian-rasterization`、`simple-knn`、GLM 等。
- `external/bin/`：项目本地可执行文件软链接，例如 `external/bin/blender`。
- `external/blender-4.2.0-linux-x64/`：Blender 4.2.0 解压目录。
- `external/blender-4.2.0-linux-x64.tar.xz`：Blender 安装包。
- `external/downloads/`：外部依赖下载暂存目录。
- `external/gaussian-splatting/`：GraphDeco 3D Gaussian Splatting 代码，用于背景 3DGS 训练、render、metrics。
- `external/git_deps/`：通过 git 安装的依赖源码暂存目录。
- `external/lerobot/`：LeRobot 代码，用于 ACT 训练。
- `external/threestudio/`：threestudio 代码，用于 SDS text-to-3D 和 Zero123 image-to-3D。
- `external/threestudio/load/zero123/zero123-xl.ckpt`：Zero123 XL 权重，已下载到项目本地。
- `external/zero123/`：Zero123 相关外部代码或旧依赖目录。

## `logs/` 文件说明

日志文件主要用于追踪安装、训练、渲染、检查过程。关键日志如下：

- `logs/3dgs_background_flowers_full.log`：Flowers 背景 3DGS 30K 训练、render、metrics 完整日志。
- `logs/3dgs_background_smoke.log`：背景 3DGS 10-step smoke test 日志。
- `logs/object_b_sds_full.log`：Object B SDS 10K 正式训练日志。
- `logs/object_c_zero123_full.log`：Object C Zero123 600-step 正式训练日志。
- `logs/fuse_scene_full_task1.log`：最终完整融合渲染日志，确认背景、A、B、C 四个资产均导入。
- `logs/check_project_final.log`：最终项目检查日志，结果为 `Project check passed.`。
- `logs/check_real_3d_env_final.log`：最终 COLMAP/Blender/threestudio/CUDA 扩展环境检查日志。
- `logs/download_zero123_xl.log`：Zero123 XL 权重下载日志。
- `logs/setup_*.log`：各类依赖安装日志。
- `logs/install_*.log`：3DGS、threestudio、CUDA 扩展安装日志。
- `logs/fuse_scene_real_b.log`、`logs/fuse_scene_real_b_real_c.log`：中间融合测试日志。
- `logs/export_object_b_sds_smoke.log`、`logs/smoke_object_b_sds.log`：Object B smoke/export 测试日志。
- `logs/diff_gaussian_heads.log`、`logs/download_diff_gaussian_*.log`、`logs/download_glm.log`：3DGS rasterizer/GLM 下载和版本排查日志。


## 各任务运行环境说明

这一节说明每个任务应该在哪个环境下运行。服务器没有 sudo 权限，所以所有依赖都安装在项目目录或用户 Conda 环境中。

### 通用环境变量

- 项目根目录固定为：`/mnt/data/kw/hy/projects/course/DL/hw3`
- 通用环境文件：`.env.hw3_setup`
- `.env.hw3_setup` 负责设置：
  - `HW3_ROOT=/mnt/data/kw/hy/projects/course/DL/hw3`
  - `TMPDIR=$HW3_ROOT/.cache/tmp`
  - `HF_HOME=$HW3_ROOT/.cache/huggingface`
  - `HUGGINGFACE_HUB_CACHE=$HW3_ROOT/.cache/huggingface/hub`
  - `TORCH_HOME=$HW3_ROOT/.cache/torch`
  - `PIP_CACHE_DIR=$HW3_ROOT/.cache/pip`
  - `CONDA_PKGS_DIRS=$HW3_ROOT/.cache/conda_pkgs`
  - `XDG_CACHE_HOME=$HW3_ROOT/.cache/xdg`
  - `PATH=$HW3_ROOT/external/bin:$HOME/bin:$PATH`
  - `HF_ENDPOINT=https://hf-mirror.com`
- 作用：把下载、模型缓存、临时文件尽量放到项目目录下，避免写系统目录或用户全局缓存。

### 第一题：COLMAP / Blender 检查与融合渲染环境

- 环境脚本：`scripts/env_colmap_blender.sh`
- Conda 环境：`.conda_envs/colmap_blender`
- 主要用途：
  - 运行 COLMAP 相关命令。
  - 运行 Blender 4.2.0。
  - 运行 `ffmpeg`、`pyyaml` 等融合渲染辅助工具。
- 关键依赖：
  - COLMAP 3.13.0
  - Blender 4.2.0：`external/bin/blender`
  - ffmpeg
  - pyyaml
- 激活方式：

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/hy/projects/course/DL/hw3/.conda_envs/colmap_blender
source scripts/env_colmap_blender.sh
```

- 相关任务：
  - `bash scripts/fuse_scene_blender.sh`
  - `bash scripts/check_project.sh`
  - 从融合视频中抽帧生成报告图片。
- GPU：Blender 当前渲染脚本主要使用普通 Blender 渲染流程，不依赖特定 GPU；如果服务器环境支持，也可以由 Blender 自行使用可用设备。

### 第一题：背景 Flowers 3DGS 训练环境

- 环境脚本：`scripts/env_threestudio.sh`
- Conda 环境：`.conda_envs/threestudio`
- 实际训练命令中使用的 GPU：`CUDA_VISIBLE_DEVICES=3`
- 主要用途：
  - 运行 GraphDeco gaussian-splatting 的 `train.py`、`render.py`、`metrics.py`。
  - 训练 Flowers 背景 3DGS 到 30,000 iteration。
- 关键依赖：
  - PyTorch 2.5.1 + CUDA 12.1 wheel
  - CUDA toolkit 路径：`CUDA_HOME=/usr/local/cuda`
  - `diff_gaussian_rasterization`，使用支持 `antialiasing` 的 `dr_aa` 分支编译
  - `simple_knn`
  - `plyfile`
  - `numpy==1.26.4`
  - `external/gaussian-splatting/`
- 额外环境变量：
  - `TCNN_CUDA_ARCHITECTURES=80`，适配 A800/A100 的 sm80 架构。
  - `LD_LIBRARY_PATH` 包含 PyTorch 的 `torch/lib`，否则 `simple_knn` 可能找不到 `libc10.so`。
- 激活方式：

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/hy/projects/course/DL/hw3/.conda_envs/threestudio
source scripts/env_threestudio.sh
```

- 复现命令：

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/hy/projects/course/DL/hw3/.conda_envs/threestudio
source scripts/env_threestudio.sh
CUDA_VISIBLE_DEVICES=3 python external/gaussian-splatting/train.py \
  -s data/360_extra_scenes/flowers \
  -m outputs/3dgs_background/flowers \
  --iterations 30000
python external/gaussian-splatting/render.py -m outputs/3dgs_background/flowers
python external/gaussian-splatting/metrics.py -m outputs/3dgs_background/flowers
```

- 封装脚本：`bash scripts/run_3dgs_background.sh flowers`
- 输出：`outputs/3dgs_background/flowers/point_cloud/iteration_30000/point_cloud.ply`

### 第一题：Object A 3DGS / 点云资产环境

- 运行环境：同背景 3DGS，使用 `scripts/env_threestudio.sh` 和 `.conda_envs/threestudio`。
- 主要用途：生成或复用 Object A 的 3DGS/点云资产。
- 封装脚本：`scripts/run_object_a_3dgs.sh`
- 当前已完成输出：`outputs/object_a/point_cloud/iteration_30000/point_cloud.ply`
- 说明：当前融合使用的是该 PLY 点云作为 Object A 资产。

### 第一题：Object B SDS text-to-3D 环境

- 环境脚本：`scripts/env_threestudio.sh`
- Conda 环境：`.conda_envs/threestudio`
- 实际完整训练使用过的 GPU：GPU 1；复现时可通过 `GPU=<id>` 指定任意空闲卡。
- 主要用途：
  - 运行 threestudio 的 DreamFusion/SDS text-to-3D。
  - 从 prompt 生成 Object B，并导出 OBJ/MTL。
- 关键依赖：
  - threestudio：`external/threestudio/`
  - PyTorch CUDA
  - tiny-cuda-nn：`tinycudann`
  - `nvdiffrast`
  - `envlight`
  - Stable Diffusion 模型：`sd-legacy/stable-diffusion-v1-5`
  - Hugging Face 缓存：`.cache/huggingface/`
- 配置文件：`configs/text_to_3d.yaml`
- 封装脚本：`scripts/run_threestudio_text3d.sh`
- 复现命令：

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/hy/projects/course/DL/hw3/.conda_envs/threestudio
GPU=1 bash scripts/run_threestudio_text3d.sh 10000
```

- 输出：
  - `outputs/text_asset_b_real/export/model.obj`
  - `outputs/text_asset_b_real/export/model.mtl`
  - `outputs/text_asset_b_real/object_b_sds/robot_prompt@20260619-144634/save/it10000-test.mp4`

### 第一题：Object C Zero123 image-to-3D 环境

- 环境脚本：`scripts/env_threestudio.sh`
- Conda 环境：`.conda_envs/threestudio`
- 实际完整训练使用过的 GPU：GPU 2；复现时可通过 `GPU=<id>` 指定任意空闲卡。
- 主要用途：
  - 运行 threestudio Zero123 image-to-3D。
  - 从 `assets/object_c/input.png` 生成 Object C，并导出 OBJ/MTL。
- 关键依赖：
  - threestudio：`external/threestudio/`
  - Zero123 XL 权重：`external/threestudio/load/zero123/zero123-xl.ckpt`
  - PyTorch CUDA
  - tiny-cuda-nn：`tinycudann`
  - `nvdiffrast`
  - `envlight`
- 封装脚本：`scripts/run_zero123_image3d.sh`
- 复现命令：

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/hy/projects/course/DL/hw3/.conda_envs/threestudio
GPU=2 bash scripts/run_zero123_image3d.sh assets/object_c/input.png 600
```

- 输出：
  - `outputs/image_asset_c_real/export/model.obj`
  - `outputs/image_asset_c_real/export/model.mtl`
  - `outputs/image_asset_c_real/object_c_zero123/chair_image@20260619-151900/save/it600-test.mp4`

### 第一题：最终融合渲染环境

- 环境脚本：`scripts/env_colmap_blender.sh`
- Conda 环境：`.conda_envs/colmap_blender`
- Blender：`external/bin/blender`，实际版本 4.2.0
- 主要用途：
  - 读取 `configs/fusion_scene.yaml`。
  - 由 `src/hw3_spatial_ai/scene/write_blender_fusion.py` 生成 Blender Python 脚本。
  - 导入背景 PLY、Object A PLY、Object B OBJ、Object C OBJ。
  - 输出 `.blend` 和 `.mp4`。
- 复现命令：

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/hy/projects/course/DL/hw3/.conda_envs/colmap_blender
bash scripts/fuse_scene_blender.sh
```

- 输出：
  - `outputs/fusion/fuse_and_render.py`
  - `outputs/fusion/fusion_scene.blend`
  - `outputs/fusion/fusion_walkthrough.mp4`

### 第二题：ACT / LeRobot 训练环境

- Conda 环境：`tom`，路径为 `/mnt/data/kw/anaconda3/envs/tom`。
- Python 版本：训练日志显示使用 Python 3.10.18。
- 运行方式：先进入 `tom` 环境，再设置 `PYTHONPATH=src`。
- 主要用途：
  - 运行 LeRobot 的 `lerobot-train`。
  - 训练 ACT-A 和 ACT-ABC。
  - 在环境 D demonstrations 上做 offline evaluation。
- 关键依赖：
  - LeRobot：`external/lerobot/` 以 editable 方式安装或在 `tom` 环境中可调用。
  - PyTorch CUDA。
  - CALVIN/LeRobot 数据：`data/calvin_task_ABC_D/`。
  - WandB 使用 offline/local 模式。
- 注意：`scripts/train_act_env_a.sh` 和 `scripts/train_act_env_abc.sh` 本身只设置 `PYTHONPATH`，不负责 `conda activate /mnt/data/kw/anaconda3/envs/tom`。所以从新 shell 复现时，应先执行：

```bash
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/anaconda3/envs/tom
cd /mnt/data/kw/hy/projects/course/DL/hw3
export PYTHONPATH=$PWD/src:${PYTHONPATH:-}
```

- ACT-A 复现命令：

```bash
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/anaconda3/envs/tom
WANDB_MODE=offline CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src \
python -m hw3_spatial_ai.lerobot.launch_act --config configs/act_env_a_real.yaml
```

- ACT-ABC 复现命令：

```bash
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/anaconda3/envs/tom
WANDB_MODE=offline CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src \
python -m hw3_spatial_ai.lerobot.launch_act --config configs/act_env_abc_real.yaml
```

- 当前已完成输出：
  - `outputs/act_env_a/checkpoints/050000/pretrained_model/model.safetensors`
  - `outputs/act_env_abc/checkpoints/050000/pretrained_model/model.safetensors`

### 第二题：ACT 环境 D 离线评估环境

- Conda 环境：同 ACT 训练，使用 `tom`。
- 运行方式：`conda activate /mnt/data/kw/anaconda3/envs/tom` 后设置 `PYTHONPATH=src`。
- 评估数据：`data/calvin_task_ABC_D/calvin_task_ABC_D_lerobot_3_4`
- 评估脚本：`src/hw3_spatial_ai/lerobot/offline_eval_act.py`
- ACT-A 评估命令：

```bash
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/anaconda3/envs/tom
CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src \
python -m hw3_spatial_ai.lerobot.offline_eval_act \
  --checkpoint outputs/act_env_a/checkpoints/050000 \
  --eval-dataset-root data/calvin_task_ABC_D/calvin_task_ABC_D_lerobot_3_4 \
  --output-dir outputs/eval_env_d_act_a --batch-size 32 --num-workers 16
```

- ACT-ABC 评估命令：

```bash
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/anaconda3/envs/tom
CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src \
python -m hw3_spatial_ai.lerobot.offline_eval_act \
  --checkpoint outputs/act_env_abc/checkpoints/050000 \
  --eval-dataset-root data/calvin_task_ABC_D/calvin_task_ABC_D_lerobot_3_4 \
  --output-dir outputs/eval_env_d_act_abc --batch-size 32 --num-workers 16
```

- 输出：
  - `outputs/eval_env_d_act_a/offline_metrics.json`
  - `outputs/eval_env_d_act_a/offline_loss_by_batch.csv`
  - `outputs/eval_env_d_act_abc/offline_metrics.json`
  - `outputs/eval_env_d_act_abc/offline_loss_by_batch.csv`

### 报告编译环境

- 推荐环境：系统/base 环境或任意包含 LaTeX 的 shell。
- 使用命令：

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3/report
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
```

- 输出：`report/main.pdf`

### 环境检查任务

- 3D 工具链检查：

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/hy/projects/course/DL/hw3/.conda_envs/threestudio
bash scripts/check_real_3d_env.sh
```

- 该脚本会检查：
  - COLMAP
  - Blender
  - PyTorch CUDA
  - threestudio
  - tinycudann / nvdiffrast / envlight

- 项目完整性检查：

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/hy/projects/course/DL/hw3/.conda_envs/colmap_blender
bash scripts/check_project.sh
```

- 最终记录：`logs/check_project_final.log` 显示 `Project check passed.`。

## `outputs/` 目录说明

- `outputs/3dgs_background/flowers/`：Flowers 背景 3DGS 正式输出。
  - `point_cloud/iteration_30000/point_cloud.ply`：最终背景 Gaussian PLY。
  - `train/ours_30000/renders/`：训练视角渲染结果。
  - `results.json`：metrics 输出；当前为 NaN，原因见“仍需注意”。
  - `cameras.json`、`cfg_args`、`exposure.json`、`input.ply`：3DGS 训练配置、中间点云和相机信息。
- `outputs/3dgs_background/flowers_smoke/`：背景 3DGS smoke test 输出。
- `outputs/object_a/`：Object A 点云输出，核心文件为 `point_cloud/iteration_30000/point_cloud.ply`。
- `outputs/text_asset_b/`：Object B 的早期/proxy 输出，保留用于对比。
- `outputs/text_asset_b_real/`：Object B 真实 SDS 输出。
  - `export/model.obj`、`export/model.mtl`：提交/融合用 OBJ 资产。
  - `object_b_sds/robot_prompt@.../`：SDS 训练、validation、test、export 过程目录。
- `outputs/image_asset_c/`：Object C 的早期/proxy 输出，保留用于对比。
- `outputs/image_asset_c_real/`：Object C 真实 Zero123 输出。
  - `export/model.obj`、`export/model.mtl`：提交/融合用 OBJ 资产。
  - `object_c_zero123/chair_image@.../`：Zero123 训练、validation、test、export 过程目录。
- `outputs/fusion/`：最终融合输出。
  - `fusion_scene.blend`：包含背景 3DGS、Object A、Object B、Object C 的 Blender 场景。
  - `fusion_walkthrough.mp4`：最终融合视频。
  - `fuse_and_render.py`：由脚本生成的 Blender Python 渲染脚本。
- `outputs/act_env_a/`：ACT-A 训练输出，包含 20K、40K、50K checkpoints。
- `outputs/act_env_abc/`：ACT-ABC 训练输出，包含 20K、40K、50K checkpoints。
- `outputs/eval_env_d_act_a/`：ACT-A 在环境 D 上的离线评估结果。
- `outputs/eval_env_d_act_abc/`：ACT-ABC 在环境 D 上的离线评估结果。
- `outputs/eval_env_d_act_a_smoke/`：ACT-A 离线评估 smoke test 结果。
- `outputs/_smoke_act_env_a_*`：ACT 训练 smoke test 输出，用于排查环境和依赖。
- `outputs/build_abc.log`：构建 A/B/C 合并数据时的日志。
- `outputs/act_env_a_train.log`：ACT-A 正式训练日志。
- `outputs/act_env_a_train_old_4k.log`：早期中断/旧训练日志。
- `outputs/act_env_abc_train.log`：ACT-ABC 正式训练日志。
- `outputs/eval_env_d_act_a.log`、`outputs/eval_env_d_act_abc.log`：环境 D 离线评估日志。

## `processed/` 目录说明

- `processed/calvin_lerobot/`：CALVIN 数据转换到 LeRobot 格式时的中间处理产物或缓存。

## `report/` 文件说明

- `report/main.tex`：报告 LaTeX 源文件。
- `report/main.pdf`：最终 PDF 报告。
- `report/main.aux`、`report/main.log`、`report/main.out`：LaTeX 编译中间文件。
- `report/figures/fusion_pipeline.png`：第一题流程图。
- `report/figures/background_3dgs_real.png`：Flowers 背景 3DGS 渲染示例。
- `report/figures/first_task_assets.png`：第一题资产概览图。
- `report/figures/fusion_final_start.png`、`fusion_final_mid.png`、`fusion_final_end.png`：最终融合视频抽帧。
- `report/figures/fusion_preview_start.png`、`fusion_preview_mid.png`、`fusion_preview_end.png`：早期融合 preview 抽帧，保留作对比。
- `report/figures/object_b_sds_real.png`：Object B SDS 真实训练结果图。
- `report/figures/object_b_text3d_proxy.png`：Object B 早期 proxy 图，保留作对比。
- `report/figures/object_c_zero123_real.png`：Object C Zero123 训练视图图。
- `report/figures/act_env_a_loss.png`：ACT-A 训练 loss 曲线。
- `report/figures/act_env_a_grad_norm.png`：ACT-A gradient norm 曲线。
- `report/figures/act_env_a_data_time.png`：ACT-A data loading time 曲线。
- `report/figures/act_env_abc_loss.png`：ACT-ABC 训练 loss 曲线。
- `report/figures/act_env_abc_grad_norm.png`：ACT-ABC gradient norm 曲线。
- `report/figures/act_env_abc_data_time.png`：ACT-ABC data loading time 曲线。
- `report/figures/act_env_abc_eval_d_loss.png`：ACT-ABC 在 D 上离线评估 batch loss 曲线。
- `report/tables/act_env_a_train_metrics.csv`：ACT-A 训练指标表。
- `report/tables/act_env_abc_train_metrics.csv`：ACT-ABC 训练指标表。
- `report/tables/act_results.csv`：ACT-A 与 ACT-ABC 在 D 上的离线评估汇总表。

## `scripts/` 文件说明

- `scripts/check_project.sh`：项目完整性检查脚本，会检查路径、资产和 Blender 融合脚本生成等。
- `scripts/check_real_3d_env.sh`：检查 COLMAP、Blender、threestudio、PyTorch/CUDA 扩展是否可用。
- `scripts/env_colmap_blender.sh`：激活 COLMAP/Blender 环境。
- `scripts/env_threestudio.sh`：激活 threestudio/3DGS 环境，并设置 CUDA、LD_LIBRARY_PATH、PYTHONPATH、缓存路径。
- `scripts/eval_act_env_d.sh`：运行 ACT 在环境 D 上的离线评估。
- `scripts/fuse_scene_blender.sh`：根据 `configs/fusion_scene.yaml` 生成 Blender 脚本并渲染融合视频。
- `scripts/run_3dgs_background.sh`：运行 Flowers 背景 3DGS 训练、render、metrics。
- `scripts/run_object_a_3dgs.sh`：运行 Object A 3DGS 或点云重建流程。
- `scripts/run_threestudio_text3d.sh`：运行 Object B SDS text-to-3D 训练和 OBJ export。
- `scripts/run_zero123_image3d.sh`：运行 Object C Zero123 image-to-3D 训练和 OBJ export。
- `scripts/setup_external_repos.sh`：拉取或准备外部依赖仓库。
- `scripts/train_act_env_a.sh`：启动 ACT-A 训练。
- `scripts/train_act_env_abc.sh`：启动 ACT-ABC 训练。

## `src/` 源码说明

- `src/hw3_spatial_ai/__init__.py`：项目 Python package 初始化文件。
- `src/hw3_spatial_ai/config.py`：读取/处理 YAML 配置和路径的工具。
- `src/hw3_spatial_ai/calvin/prepare_lerobot_splits.py`：准备 CALVIN 到 LeRobot split 的脚本。
- `src/hw3_spatial_ai/calvin/build_abc_lerobot.py`：构建 A/B/C 合并训练数据。
- `src/hw3_spatial_ai/calvin/inspect_dataset.py`：检查 CALVIN/LeRobot 数据集内容。
- `src/hw3_spatial_ai/lerobot/launch_act.py`：根据配置拼接并启动 `lerobot-train` 的封装脚本。
- `src/hw3_spatial_ai/lerobot/launch_act.py.bak`：`launch_act.py` 的备份文件。
- `src/hw3_spatial_ai/lerobot/eval_act.py`：ACT 评估相关代码。
- `src/hw3_spatial_ai/lerobot/offline_eval_act.py`：在环境 D demonstrations 上做 offline action loss evaluation。
- `src/hw3_spatial_ai/lerobot/summarize_runs.py`：汇总训练/评估结果并生成报告用图表。
- `src/hw3_spatial_ai/scene/find_mipnerf_scene.py`：定位 Mip-NeRF 360 场景数据。
- `src/hw3_spatial_ai/scene/write_blender_fusion.py`：根据融合配置写出 Blender Python 脚本。
- `src/**/__pycache__/`：Python 运行产生的缓存文件，无需提交。

## 复现命令

### 环境检查

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/hy/projects/course/DL/hw3/.conda_envs/threestudio
bash scripts/check_real_3d_env.sh
bash scripts/check_project.sh
```

### 第一题

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
bash scripts/run_3dgs_background.sh flowers
GPU=0 bash scripts/run_threestudio_text3d.sh 10000
GPU=0 bash scripts/run_zero123_image3d.sh assets/object_c/input.png 600
bash scripts/fuse_scene_blender.sh
```

### 第二题

```bash
cd /mnt/data/kw/hy/projects/course/DL/hw3
bash scripts/train_act_env_a.sh
bash scripts/train_act_env_abc.sh
bash scripts/eval_act_env_d.sh
```

也可以直接用 Python module 方式：

```bash
source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/anaconda3/envs/tom
WANDB_MODE=offline CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src \
python -m hw3_spatial_ai.lerobot.launch_act --config configs/act_env_a_real.yaml

source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/anaconda3/envs/tom
WANDB_MODE=offline CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src \
python -m hw3_spatial_ai.lerobot.launch_act --config configs/act_env_abc_real.yaml

source /mnt/data/kw/anaconda3/etc/profile.d/conda.sh
conda activate /mnt/data/kw/anaconda3/envs/tom
CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src \
python -m hw3_spatial_ai.lerobot.offline_eval_act \
  --checkpoint outputs/act_env_abc/checkpoints/050000 \
  --eval-dataset-root data/calvin_task_ABC_D/calvin_task_ABC_D_lerobot_3_4 \
  --output-dir outputs/eval_env_d_act_abc --batch-size 32 --num-workers 16
```

## 最终检查记录

- `logs/check_project_final.log` 显示：`Project check passed.`
- `logs/check_real_3d_env_final.log` 显示：COLMAP、Blender、threestudio、PyTorch CUDA、tinycudann、nvdiffrast、envlight 可用。
- 最终检查时 8 张 GPU 均为空闲。
- 第一题最终融合配置为 `configs/fusion_scene.yaml`，四个资产路径均指向真实输出。
